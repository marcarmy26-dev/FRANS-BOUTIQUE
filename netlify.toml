[build]
  publish = "."
  functions = "netlify/functions"

# SPA routing: every path serves index.html so /find, /studio,
# /shelf, /story work on direct load and refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
